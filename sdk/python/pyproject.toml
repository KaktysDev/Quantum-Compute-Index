[project]
name = "qrouter"
version = "0.1.0"
description = "Python client for the QRouter universal quantum execution API"
requires-python = ">=3.9"
dependencies = ["httpx>=0.27"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
