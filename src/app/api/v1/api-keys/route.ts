import{NextResponse}from"next/server";import{createApiKey,resolvePrincipal}from"@/lib/qrouter/auth";import{apiError}from"@/lib/qrouter/http";import{createAdminClient}from"@/lib/supabase/admin";
export async function GET(request:Request){try{const p=await resolvePrincipal(request);if(p.demo)return NextResponse.json({object:"list",data:[{id:"demo",name:"Local development",key_prefix:"qci_test_local...",environment:"test",created_at:new Date().toISOString()}]});const{data,error}=await createAdminClient().from("api_keys").select("id,name,key_prefix,environment,scopes,last_used_at,expires_at,revoked_at,created_at").eq("organization_id",p.organizationId).order("created_at",{ascending:false});if(error)throw error;return NextResponse.json({object:"list",data});}catch(e){return apiError(e)}}
export async function POST(request:Request){try{const p=await resolvePrincipal(request);if(!p.userId)return NextResponse.json({error:{type:"forbidden",message:"API keys can only be created from the console."}},{status:403});const body=await request.json()as{name?:string;environment?:"test"|"live"},g=createApiKey(body.environment??"live");if(p.demo)return NextResponse.json({id:"demo",name:body.name??"Developer key",key:"qci_test_local_development",key_prefix:"qci_test_local..."},{status:201});const{data,error}=await createAdminClient().from("api_keys").insert({organization_id:p.organizationId,created_by:p.userId,name:body.name?.trim()||"Developer key",environment:body.environment??"live",key_prefix:g.prefix,key_hash:g.hash}).select("id,name,key_prefix,environment,created_at").single();if(error)throw error;return NextResponse.json({...data,key:g.key},{status:201});}catch(e){return apiError(e)}}

