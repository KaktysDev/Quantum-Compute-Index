import{NextResponse}from"next/server";import{resolvePrincipal}from"@/lib/qrouter/auth";import{apiError}from"@/lib/qrouter/http";import{createAdminClient}from"@/lib/supabase/admin";
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{const p=await resolvePrincipal(request),{id}=await params;if(p.demo)return new NextResponse(null,{status:204});const{error}=await createAdminClient().from("webhook_endpoints").delete().eq("id",id).eq("organization_id",p.organizationId);if(error)throw error;return new NextResponse(null,{status:204})}catch(e){return apiError(e)}}
